export default function LoadingPage() {
  return (
    <main className="shell">
      <section className="loading-state">
        <p className="eyebrow">Loading dashboard</p>
        <h1>AI Trend Hunter</h1>
        <p>Fetching trend signals and recent pipeline runs...</p>
      </section>
    </main>
  );
}
